{
  "name": "tesnim-tag-generator",
  "version": "1.0.0",
  "description": "Price tag generator for TESNIM products",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../api && npm install",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd backend && npm install && node download-fonts.js && cd ../api && npm install && cd ../frontend && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

